import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  background: rgba(228, 205, 190, 0.3);
  font-family: Open Sans,serif;
  
   span {
     display: flex;
     margin: 100px auto 100px auto;
   }
`;
export const Grid = styled.div`

`
